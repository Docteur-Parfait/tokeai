# Streamlit configuration for TokeAI
[server]
headless = true
port = 8080
address = "0.0.0.0"

[theme]
# Optional: customize later
# primaryColor = "#..."
# backgroundColor = "#..."
# secondaryBackgroundColor = "#..."
